{
  "name": "@rxdi/ui-kit-client",
  "version": "1.0.0",
  "scripts": {
    "build": "npx firelink build ./index.html --out-dir ../docs"
  },
  "browserslist": [
    "last 1 chrome versions"
  ],
  "dependencies": {
    "@rxdi/ui-kit": "1.0.0"
  },
  "fireConfig": {
    "runner": "parcel"
  },
  "fireDependencies": {
    "@rxdi/ui-kit": "../src/"
  },
  "devDependencies": {
    "@types/node": "^12.0.8"
  }
}